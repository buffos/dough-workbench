# Composition and Intrinsic Metrics — Orchestration Status

State: `bounded`

The capability owns functional composition, deterministic metrics, estimated
physical quantities, and pre-process heuristic metrics. It must maintain the
calculated/estimated/heuristic distinction and the `Unknown != 0` invariant.

Next action: tighten metric contracts and missing-data behavior through the
architecture-spec workflow. No delivery issue has been created.
