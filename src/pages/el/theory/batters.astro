---
import LocaleLayout from '../../../layouts/LocaleLayout.astro';
import BattersTheoryPage from '../../../components/BattersTheoryPage.svelte';

const locale = 'el';
const basePath = import.meta.env.BASE_URL;
---

<LocaleLayout locale={locale} basePath={basePath}>
  <BattersTheoryPage client:load locale={locale} basePath={basePath} />
</LocaleLayout>
